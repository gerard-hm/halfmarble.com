# OS Metadata
.DS_Store
Thumbs.db

# Local Environment / Secrets
.env
.env.local
*.log

# Editor / IDE Config
.vscode/
.idea/
*.swp

# Build / Dependency Folders (if you add them later)
node_modules/
dist/
build/

# Local-only Claude Code guidance (canonical copy lives in halfmarble.closed/)
CLAUDE.md

# ViroFlick media — the site ships the sized .jpg screenshots, the poster, and the
# one small self-hosted gameplay loop (media/loop-*.mp4, a couple of MB). The heavy
# source assets stay local: the multi-GB .mp4 capture archive and the intermediate
# cuts the loop was encoded from, plus the full-res .png captures behind the .jpgs.
# Keep it that way — git keeps every blob forever and each re-encode compounds.
viroflick/media/videos/
viroflick/media/originals/
